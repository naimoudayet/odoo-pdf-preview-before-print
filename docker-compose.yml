name: pdfprev-18

services:
  db:
    image: postgres:16
    container_name: pdfprev-pg-18
    environment:
      POSTGRES_USER: odoo
      POSTGRES_PASSWORD: odoo
      POSTGRES_DB: postgres
    volumes:
      - db-data:/var/lib/postgresql/data

  odoo:
    build: .
    container_name: pdfprev-odoo-18
    depends_on:
      - db
    ports:
      - "2018:8069"
    environment:
      HOST: db
      USER: odoo
      PASSWORD: odoo
    volumes:
      - odoo-data:/var/lib/odoo
      - ./no_pdf_preview_print:/mnt/extra-addons/no_pdf_preview_print

volumes:
  db-data:
  odoo-data:
